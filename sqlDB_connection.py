#from all_imports import *

#Create connection between dataframe and postgreSQL ( OPTIONALL)
# from sqlalchemy import create_engine
# load_dotenv()
# DATABASE_URL = os.getenv("DB_URL")
# engine = create_engine(DATABASE_URL)
# df.to_sql('uci_heart_disease', engine, if_exists='replace', index=False)
# print("df was moved to postgreSQL successfully")
